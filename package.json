{
  "name": "studyflow-ai-root",
  "version": "1.0.0",
  "description": "Root package for managing frontend and backend startup",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "start": "npx concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
